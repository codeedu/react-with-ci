// @flow 
import * as React from 'react';
interface HeaderProps {
    
};
export const Header: React.FC<HeaderProps> = (props: Props) => {
    return (
        <div className="Header">
            
        </div>
    );
};