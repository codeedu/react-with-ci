// @flow 
import * as React from 'react';
import './index.css';

interface ChartProps {
    
};
export const Chart: React.FC<ChartProps> = (props) => {
    return (
        <div>
            
        </div>
    );
};